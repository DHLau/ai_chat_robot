import 'package:flutter/material.dart';

class MenuViewLoggedIn extends StatelessWidget {
  const MenuViewLoggedIn({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.green);
  }
}
